---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="CLI 参考" description="RelayCore 命令行接口">
  <h1>CLI 参考</h1>

  <p><code>relay-core-cli</code> 二进制文件提供了功能完整的命令行界面，用于运行代理和管理流量、规则和证书。</p>

  <h2>命令</h2>

  <h3>run</h3>
  <p>启动代理服务器。</p>
  <pre><code>relay-core-cli run [options]

Options:
  -l, --listen &lt;ADDR&gt;     代理监听地址（默认：127.0.0.1:8080）
  -c, --control-port &lt;P&gt;  控制 API 端口（默认：8081）
  --api-port &lt;P&gt;           启用 REST/SSE HTTP API 端口
  --api-token &lt;TOKEN&gt;      HTTP API Bearer 认证令牌
  --api-cors &lt;ORIGINS&gt;     CORS 允许的源
  --ui                    启用 TUI 交互界面
  --rules &lt;PATH&gt;          从文件加载规则（JSON/YAML）
  --script &lt;PATH&gt;         加载 Deno 脚本
  --script-watch           监听脚本文件变更自动重载
  --script-env-allow &lt;K&gt;  允许脚本访问的环境变量（逗号分隔）
  --transparent            启用透明代理模式</code></pre>

  <h3>ca</h3>
  <p>管理用于 HTTPS 拦截的 CA 证书。</p>
  <pre><code>relay-core-cli ca &lt;subcommand&gt;

Subcommands:
  generate               生成 CA 证书与私钥（默认 ~/.relay-core/）
  install                将 CA 安装到系统信任存储（macOS）
  status                 显示 CA 路径、存在性与信任状态
  export                 导出 CA 证书
  uninstall              从系统信任存储移除 CA（macOS）</code></pre>

  <p>可选路径：<code>--ca-cert</code> 与 <code>--ca-key</code> 须成对提供；亦可通过 <code>RELAY_CA_CERT</code> / <code>RELAY_CA_KEY</code> 设置。</p>

  <h3>rules</h3>
  <p>管理拦截规则。</p>
  <pre><code>relay-core-cli rules &lt;subcommand&gt;

Subcommands:
  validate &lt;file&gt;        验证规则文件格式
  print &lt;file&gt;           打印规则文件（JSON/YAML）
  test &lt;file&gt; --flow F   用样本流测试规则匹配
  list --api-url &lt;URL&gt;   列出当前运行代理中的活跃规则</code></pre>

  <h3>flows</h3>
  <p>查询捕获的流量（需要代理运行并启用 --api-port）。</p>
  <pre><code>relay-core-cli flows [options]

Options:
  --api-url &lt;URL&gt;        API 地址（默认：http://127.0.0.1:8082）
  --host &lt;HOST&gt;          按主机名过滤
  --path &lt;PATH&gt;          按路径过滤
  --method &lt;METHOD&gt;      按 HTTP 方法过滤
  --status-min &lt;N&gt;       最小状态码
  --status-max &lt;N&gt;       最大状态码
  --filter &lt;EXPR&gt;        过滤表达式（如 host:api method:POST）
  --format &lt;json|table&gt;  输出格式</code></pre>

  <h3>metrics</h3>
  <p>显示运行时指标。</p>
  <pre><code>relay-core-cli metrics [options]

Options:
  --proxy-url &lt;URL&gt;      代理地址（默认：http://127.0.0.1:8080）
  --output &lt;table|json&gt;  输出格式（默认：table）</code></pre>

  <h3>scripts</h3>
  <p>管理用于请求/响应修改的 Deno 脚本。</p>
  <pre><code>relay-core-cli scripts &lt;subcommand&gt;

Subcommands:
  list                     列出已加载的脚本
  load &lt;path&gt;              加载脚本
  unload &lt;name&gt;            卸载脚本</code></pre>

  <h2>过滤表达式</h2>

  <p>使用表达式语法过滤流量：</p>

  <pre><code># 按方法
method:GET
method:POST

# 按主机（支持通配符）
host:example.com
host:*.example.com

# 按路径
path:/api/users
path:/api/v2/*

# 按状态码
status:200
status:[200 TO 299]
status:[400 TO 499]

# 使用 AND/OR 组合
method:GET AND host:*.example.com
method:POST OR method:PUT

# 取反
NOT method:OPTIONS</code></pre>

  <h2>环境变量</h2>

  <pre><code>RELAY_LOG            # 日志级别（如 info、debug）
RELAY_DATA_DIR       # 数据目录（默认 ~/.relay-core）
RELAY_CA_CERT        # CA 证书路径（须与 RELAY_CA_KEY 成对）
RELAY_CA_KEY         # CA 私钥路径</code></pre>

  <p>完整说明见 <a href="/docs/configuration">配置</a>。</p>
</DocsLayout>