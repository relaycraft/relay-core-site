---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="安装" description="RelayCore 安装指南">
  <h1>安装</h1>

  <h2>系统要求</h2>
  <ul>
    <li>Rust 1.70+（从源码编译需要）</li>
    <li>OpenSSL（Linux）或 SecureTransport（macOS）</li>
    <li>CA 证书安装需要管理员权限</li>
  </ul>

  <h2>Cargo（推荐）</h2>
  <pre><code>cargo install relay-core-cli</code></pre>

  <h2>npm</h2>
  <pre><code>npm install -g @relay-core/cli</code></pre>

  <h2>预构建二进制</h2>
  <p>从 <a href="https://github.com/relaycraft/relay-core/releases">GitHub Releases</a> 下载：</p>
  <ul>
    <li>macOS (Apple Silicon)</li>
    <li>macOS (Intel)</li>
    <li>Linux (x86_64)</li>
    <li>Linux (ARM64)</li>
    <li>Windows (x86_64)</li>
  </ul>

  <h3>Linux 安装</h3>
  <pre><code>tar -xzf relay-core-cli-x86_64-unknown-linux-gnu.tar.gz
sudo mv relay-core-cli /usr/local/bin/
relay-core-cli --version</code></pre>

  <p>Linux 预编译二进制为 <strong>glibc-linked (gnu)</strong> 构建，包含 Deno / V8 脚本引擎。<code>musl</code> 静态链接变体不再发布——因为 <code>rusty_v8</code> 没有 musl 预编译产物，发布不带 V8 的 musl 包会在 Linux 上破坏 JS 脚本功能。如需 musl，请从源码用 <code>cargo build --release --target x86_64-unknown-linux-musl</code> 自编译。</p>

  <h3>macOS 安装</h3>
  <pre><code>tar -xzf relay-core-cli-x86_64-apple-darwin.tar.gz
sudo mv relay-core-cli /usr/local/bin/
relay-core-cli --version</code></pre>

  <h3>Windows 安装</h3>
  <p>解压 ZIP 文件并将目录添加到 PATH。</p>

  <h2>从源码构建</h2>
  <pre><code>git clone https://github.com/relaycraft/relay-core
cd relay-core
cargo build --release
./target/release/relay-core-cli --version</code></pre>

  <h2>验证安装</h2>
  <pre><code>relay-core-cli --version
# relay-core-cli 0.8.3</code></pre>
</DocsLayout>