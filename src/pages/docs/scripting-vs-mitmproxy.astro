---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="脚本能力概览" description="RelayCore 脚本引擎能做什么、何时使用">
  <h1>脚本能力概览</h1>

  <p>RelayCore 脚本引擎基于 Deno/V8，使用 TypeScript 编写流量钩子。它与<a href="/docs/rule-engine">规则引擎</a>互补：规则适合声明式匹配与固定动作，脚本适合需要逻辑分支、跨请求状态或动态计算的场景。</p>

  <h2>何时用脚本、何时用规则</h2>

  <table>
    <thead>
      <tr><th>需求</th><th>推荐</th></tr>
    </thead>
    <tbody>
      <tr><td>按 URL / Method 改 Header、Mock 固定响应</td><td>规则</td></tr>
      <tr><td>根据响应内容动态改写、聚合统计、跨请求缓存</td><td>脚本</td></tr>
      <tr><td>人工断点、UI 驱动的拦截恢复</td><td>规则 + Tauri / HTTP API</td></tr>
      <tr><td>复杂业务逻辑（签名、令牌刷新、A/B 分流）</td><td>脚本</td></tr>
    </tbody>
  </table>

  <h2>生命周期钩子</h2>

  <p>脚本通过 <code>globalThis</code> 注册回调，在流量经过代理的各阶段执行：</p>

  <table>
    <thead>
      <tr><th>钩子</th><th>触发时机</th></tr>
    </thead>
    <tbody>
      <tr><td><code>onConnect</code> / <code>onDisconnect</code></td><td>TCP 连接建立与关闭</td></tr>
      <tr><td><code>onRequestHeaders</code> / <code>onResponseHeaders</code></td><td>HTTP 头可读可改</td></tr>
      <tr><td><code>onRequest</code> / <code>onResponse</code></td><td>完整请求/响应（含 body）</td></tr>
      <tr><td><code>onWebSocketMessage</code></td><td>WebSocket 帧级拦截</td></tr>
      <tr><td><code>onError</code></td><td>代理或脚本错误，含 stage 定位</td></tr>
    </tbody>
  </table>

  <p>完整 API 与类型说明见<a href="/docs/scripting">脚本 API</a>；开发流程见<a href="/docs/scripting-guide">脚本开发指引</a>。</p>

  <h2>运行时能力</h2>

  <ul>
    <li><strong>TypeScript / npm</strong>：通过 esbuild 打包后由 CLI 的 <code>--script</code> 或 HTTP <code>/api/v1/script</code> 加载</li>
    <li><strong>sharedState</strong>：跨请求共享内存状态（per-isolate 隔离，适合计数器、缓存索引）</li>
    <li><strong>relay.env</strong>：白名单控制的环境变量访问（<code>--script-env-allow</code>）</li>
    <li><strong>relay.fetch</strong>：可选的子请求能力，默认关闭，需显式开启并配置白名单</li>
    <li><strong>relay.* 工具</strong>：uuid、hash、base64、json 等常用辅助函数</li>
    <li><strong>规则上下文</strong>：<code>flow.matched_rules</code> 与 <code>flow.rule_variables</code> 让脚本感知规则命中结果</li>
    <li><strong>可观测性</strong>：脚本耗时等指标通过 Prometheus 端点暴露（见<a href="/docs/observability">监控</a>）</li>
  </ul>

  <h2>最小示例</h2>

  <pre><code>globalThis.onRequestHeaders = (_ctx, flow) => &#123;
  flow.layer.data.request.headers.push(["X-RelayCore", "1"]);
  return flow;
&#125;;

globalThis.onResponse = (_ctx, flow) => &#123;
  if (flow.response?.status === 404) &#123;
    console.warn("404:", flow.id);
  &#125;
  return flow;
&#125;;</code></pre>

  <pre><code># 加载脚本运行
relay-core-cli run --script ./hooks.ts --script-watch</code></pre>

  <h2>与其他工具的脚本模型</h2>

  <p>如果你熟悉 Python addon（如 mitmproxy）或其他代理的脚本扩展，主要差异在于：</p>

  <ul>
    <li><strong>语言</strong>：TypeScript 而非 Python；可复用 npm 生态（需 bundle）</li>
    <li><strong>Headers</strong>：使用 <code>[name, value][]</code> 数组，保留重复 header</li>
    <li><strong>Body</strong>：<code>body.content</code> 为 base64，读写需 <code>atob</code> / <code>btoa</code></li>
    <li><strong>全局状态</strong>：使用 <code>sharedState</code> 而非进程级 Python 全局变量</li>
    <li><strong>Mock 响应</strong>：优先用<a href="/docs/rule-engine">规则引擎</a>的 Mock 动作；脚本侧重动态逻辑</li>
  </ul>

  <h3>从 Python addon 迁移示例</h3>

  <pre><code># mitmproxy
def request(flow):
    flow.request.headers["X-Custom"] = "value"

# RelayCore
globalThis.onRequestHeaders = (_ctx, flow) => &#123;
  flow.layer.data.request.headers.push(["X-Custom", "value"]);
  return flow;
&#125;;</code></pre>

  <h2>多宿主一致运行</h2>

  <p>同一份脚本可在以下宿主加载，行为由共享 runtime 保证一致：</p>

  <ul>
    <li><code>relay-core-cli run --script</code></li>
    <li>HTTP API <code>POST /api/v1/script</code></li>
    <li>Tauri <code>load_script</code> command</li>
  </ul>

  <p>CLI 适合本地开发，HTTP API 适合 Web UI 或自动化，Tauri 适合桌面应用内嵌。详见<a href="/docs/architecture">架构</a>。</p>
</DocsLayout>
