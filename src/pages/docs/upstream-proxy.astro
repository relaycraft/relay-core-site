---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="上游代理" description="将 RelayCore 串接到上级代理之后">
  <h1>上游代理（Upstream Proxy）</h1>

  <p>RelayCore 可以将所有出站请求通过一个"上级（upstream）代理"转发。这是把 RelayCore 放在公司代理、TLS 检查中间盒或 VPN 集中器后面的标准做法，无需修改任何客户端配置。</p>

  <h2>快速开始</h2>

  <pre><code># 纯 HTTP 上级代理
relay-core-cli run --upstream http://corp-proxy.internal:8080

# HTTPS 上级代理（先与代理建立 TLS 连接）
relay-core-cli run --upstream https://secure-proxy.internal:8443

# 带 HTTP Basic 认证
relay-core-cli run \
  --upstream http://corp-proxy.internal:8080 \
  --upstream-auth-user alice

# 上级不可达时回落到直连
relay-core-cli run --upstream http://corp-proxy.internal:8080 --upstream-fail-open</code></pre>

  <p>Basic 认证的密码 <strong>不允许</strong> 通过命令行参数传入，避免出现在 <code>ps</code> 输出或 shell 历史中：</p>

  <pre><code>export RELAYCORE_UPSTREAM_PASSWORD='s3cret'
relay-core-cli run --upstream http://corp-proxy.internal:8080 --upstream-auth-user alice</code></pre>

  <h2>参数</h2>

  <table>
    <tr><th>参数</th><th>说明</th></tr>
    <tr><td><code>--upstream &lt;URL&gt;</code></td><td>上级代理 URL。<code>http://...</code> 使用明文 HTTP CONNECT；<code>https://...</code> 先与代理建立 TLS 再 CONNECT。</td></tr>
    <tr><td><code>--upstream-auth-user &lt;USER&gt;</code></td><td>HTTP Basic 认证用户名。密码从 <code>RELAYCORE_UPSTREAM_PASSWORD</code> 读取；只设用户名会报错。</td></tr>
    <tr><td><code>--upstream-bypass &lt;LIST&gt;</code></td><td>逗号分隔的绕过列表，匹配的流量直连而不走上级。支持 <code>cidr:10.0.0.0/8</code>、IP 字面量（<code>127.0.0.1</code>、<code>::1</code>）与 glob 主机名（<code>*.internal</code>）。</td></tr>
    <tr><td><code>--upstream-fail-open</code></td><td>上级不可达时透明回落到直连。默认关闭（fail-closed），对强制门户与公司策略更安全。</td></tr>
  </table>

  <h2>绕过规则语法</h2>

  <p><code>--upstream-bypass</code> 接受三种规则，可混用：</p>

  <pre><code>--upstream-bypass "127.0.0.1,::1,localhost,*.internal,cidr:10.0.0.0/8,cidr:192.168.0.0/16"</code></pre>

  <table>
    <tr><th>模式</th><th>匹配</th></tr>
    <tr><td><code>127.0.0.1</code></td><td>字面量 IPv4 / IPv6 地址</td></tr>
    <tr><td><code>localhost</code></td><td>字面量主机名（大小写不敏感）</td></tr>
    <tr><td><code>*.internal</code></td><td>glob —— 匹配 <code>internal</code> 的任意子域</td></tr>
    <tr><td><code>cidr:10.0.0.0/8</code></td><td>CIDR 段（IPv4 / IPv6）</td></tr>
  </table>

  <p>透明代理模式下还会将原始目的 IP 与绕过列表比对，确保到本地服务的回环与 RFC1918 流量始终直连，即使上级代理在线。</p>

  <h2>HTTP vs HTTPS 上级</h2>

  <p>当 URL 以 <code>https://</code> 开头时，RelayCore 会先与代理完成 TLS 握手，再发送 <code>CONNECT</code>。代理证书使用系统信任库（或自定义 CA 证书链）校验。在使用私有 CA 的环境中，请将该 CA 导入系统信任库，或避免与代理之间使用 TLS。</p>

  <h2>运行时变更</h2>

  <p>上游代理配置是 <code>ProxyPolicy</code> 的一部分，可通过 REST API 查看与局部更新：</p>

  <pre><code># 查看当前策略
curl -s http://127.0.0.1:8082/api/v1/policy

# 仅更新 upstream 段
curl -s -X PATCH http://127.0.0.1:8082/api/v1/policy \
  -H 'Content-Type: application/json' \
  -d '&#123;
    "upstream": &#123;
      "proxy_url": "http://corp-proxy.internal:8080",
      "bypass_hosts": ["*.internal", "127.0.0.1"],
      "fail_open": false
    &#125;
  &#125;'</code></pre>

  <p><strong>注意：</strong> 运行时切换上游会返回 <code>409 Conflict</code>，因为已有连接已经绑定在旧路径上。要应用新的上游 URL 请重启代理。</p>

  <h2>安全提示</h2>

  <ul>
    <li><code>password</code> 字段使用 <code>secrecy::SecretString</code> 包装。<code>Debug</code> 实现、<code>GET /api/v1/policy</code> 响应与 JSON 序列化器都将其掩码为 <code>***</code>。</li>
    <li>当上级代理是策略强制的，请保持 <code>--upstream-fail-open=false</code>（默认）—— 静默回落到直连会失去原本的保护。</li>
    <li>策略变更会写入 <code>/api/v1/audit</code>，<code>kind=policy_updated</code>，并记录触发该变更的角色（actor）。</li>
  </ul>
</DocsLayout>
